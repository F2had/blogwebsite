<%- include('partials/header'); %> 
<h2 class="text-center">Welcome</h2>

  <% posts.forEach(post => { %>
    <h1> <%= post.title %> </h1>
    <p> <%= post.content %> </p>
  <% }) %>

<%- include('partials/footer'); %> 